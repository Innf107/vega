-- type: print(1)

import internal:Internal (replicateMutableArray, unsafeFreezeArray, unsafeReadArray, debugInt)

main : () -> ()
main() = {
    let array = unsafeFreezeArray(replicateMutableArray(5, 1))
    debugInt(unsafeReadArray(array, 2))
}
