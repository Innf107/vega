-- type: print(1)

import internal:Internal (debugInt)

main : () -> ()
main() = {
    let x = 1
    let g(y : Bool) = if y then {
        let z = 2
        let h() = {
            g(False)
        }
    } else {
        debugInt(x)
    }
    g(True)
}

