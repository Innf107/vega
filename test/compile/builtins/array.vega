-- type: print(1)

import internal:Internal (replicateArray, unsafeFreezeArray, unsafeReadArray, debugInt)

main : () -> ()
main() = {
    let array = unsafeFreezeArray(replicateArray(5, 1))
    debugInt(unsafeReadArray(array, 2))
}
