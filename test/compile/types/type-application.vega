-- type: compile
f : forall (a : Type(Boxed)). a -> a;
f(x) = x;

g : forall (b : Type(Boxed)). b -> b;
g[b](x) = f[b](x);

ignore : forall a. a -> ()
ignore(_) = ()

main : () -> ();
main() = {
    ignore(f)
    ignore(g)
};