-- type: print(4)

import internal:Internal (debugInt)

f : () -> exists (a : Type(IntRep)). (Int -> a, a -> Int);
f() = (\_ -> 3, \x -> x + 1);

main : () -> ();
main() = {
    let (makeA, render) = f();
    let blah = makeA(5);
    debugInt(render(blah));
}

