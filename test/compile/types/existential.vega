
f : () -> exists (a : Type(IntRep)). (Int -> a, a -> String);
f() = (\_ -> 3, \_ -> "a");

main : () -> ();
main() = {
    let (makeA, render) = f();
    let blah = makeA(5);
    let _ = render(blah);
}

