-- type: compile
f : forall *(r : Rep) (a : Type(r)). a -> a;
f(x) = x;

g : forall (a : Type(Boxed)). a -> a;
g(x) = f[Boxed](x);

ignore : forall a. a -> ()
ignore(x) = ()

main : () -> ();
main() = {
    ignore(f)
    ignore(g)
};